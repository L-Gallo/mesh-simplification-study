# Core dependencies for mesh simplification benchmark
# Python 3.12+

# Mesh processing and visualization
pyvista>=0.43.0
fast-simplification>=0.1.0
open3d>=0.18.0
meshoptimizer>=0.20.0
trimesh>=4.0.0

# Geometric accuracy computation (requires rtree separately via conda)
# rtree - Install via: conda install -c conda-forge rtree

# Scientific computing
numpy>=1.26.0
scipy>=1.11.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Performance monitoring
psutil>=5.9.0

# Progress tracking (optional but recommended)
tqdm>=4.66.0

# Note on rtree:
# This package has complex system dependencies (libspatialindex)
# Installation via conda is STRONGLY recommended:
#   conda install -c conda-forge rtree
# If pip installation fails, you must use conda.
